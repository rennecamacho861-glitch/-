# ============================================================================
# ccgs.env — CCGS 框架全局环境变量（唯一真源）
# ============================================================================
# 所有 Shell 脚本、init 工具从此文件读取全局路径配置。
# 更换项目时只需修改此文件中的变量值。
#
# 用法:
#   在脚本中 source 此文件:
#   source "$PROJECT_ROOT/.ccgs-core/ccgs.env"
# ============================================================================

# --- 数据层根目录 ---
# CCGS-Data 的目录名称（相对于项目根目录）
# 如需修改，运行: bash .ccgs-core/init.sh --rename-data <新名称>
DATA_DIR="CCGS-Data"

# --- 核心引擎目录 ---
# .ccgs-core 的目录名称（一般无需修改）
CORE_DIR=".ccgs-core"
