{
  "name": "zhaomian-zhishi-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5188 --strictPort",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5188 --strictPort",
    "test": "tsc -p tsconfig.test.json && node scripts/prepare-test-build.cjs && node --test \"tests/**/*.test.mjs\" \"tests/**/*_test.mjs\""
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "vite": "^5.4.11",
    "typescript": "^5.6.3"
  },
  "devDependencies": {}
}
