# v0.6.7 删除全局危险与旧筹码危险格 QA 报告

日期：2026-05-12

## 范围

本次 QA 覆盖全局 `danger` 字段删除、危险格删除、旧筹码触发删除、HUD 状态栏删除危险显示，以及相关地图/移动/战斗回归。

## 自动化结果

命令：`npm test`

结果：通过，25/25。

覆盖点：

- 新局 `GameState` 中不存在 `danger` 字段。
- 初始地图中不存在 `danger` tile。
- 72 回合后不会生成或提高全局危险，也不会直接结束本局。
- 边缘墙地图仍保持无占格墙、无死路、全图连通、最长直线开放边限制。
- 拾取、敌人拾取、遭遇、远程伏击、掉落、敌人移动、敌人用道具等回归测试保持通过。

命令：`npm run build`

结果：通过。

备注：Vite 仍提示 Phaser 产物 chunk 大于 500 kB，这是当前原型既有打包形态，不阻塞 v0.6.7。

## 浏览器验证

目标：`http://127.0.0.1:5173/`

结果：

- 页面正常打开，标题为《照面之时》。
- HUD 不再显示“危险”。
- 控制台 error 数为 0。

## 结论

PASS。全局危险与旧筹码危险格已从运行时、地图、HUD、测试和权威文档中删除。`FeedbackEvent.tone = "danger"` 仅作为红色演出语义保留，不是玩法压力字段。
