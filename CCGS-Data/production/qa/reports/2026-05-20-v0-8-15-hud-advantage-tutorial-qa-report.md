# v0.8.15 HUD 道具折叠、优势续战反馈与新手教程 QA 报告

日期：2026-05-20  
结论：PASS

## 覆盖范围

- 背包道具按钮默认压缩展示。
- 背包道具说明与限制通过 hover/focus 展开。
- 优势窗口 `pressPower` / `pressTempo` 可在同场照面中重复兑现，只要玩家再次获得优势。
- 继续战斗选择生成 `advantage-press` 确认式反馈。
- 新手教程首次询问、启用、关闭和场景触发。
- 规则真源与架构文档同步。

## 自动化验证

- `npm test`
  - 结果：119 / 119 通过。
  - 覆盖重点：`tests/unit/combat_advantage_flee_persuasion_test.mjs` 已更新重复续战和反馈事件断言。
- `npm run build`
  - 结果：通过。
  - TypeScript 与 Vite 生产构建成功。

## 手动/运行验证

- `http://127.0.0.1:5188/` 返回 200，dev server 已启动。
- 未使用浏览器截图自动化：当前工作区未安装 Playwright；本轮以自动化测试、生产构建和本地 HTTP 可达性作为交付验证。

## 风险与测试债务

- 教程属于 HUD 本地状态，尚未覆盖自动化 UI 点击测试；后续若教程逻辑继续增长，建议新增浏览器级交互测试。
- 背包说明在桌面 hover/focus 下可用；触摸设备目前依赖点击焦点和浏览器行为，后续可做专门的触摸展开按钮。
- 本轮未重新推送 GitHub Pages 公开仓库，在线传播版本仍需后续导出与上传。

## 验收说明

本轮保持架构边界：`src/sim` 只负责优势续战规则与反馈事件；`src/main.ts` 负责 HUD、教程偏好和输入桥接；`src/styles.css` 负责折叠说明与弹窗表现。教程状态不进入 `GameState`，不会影响 seed 复现或模拟层测试。
