# QA Report：v0.7.1 优势联动战斗道具

日期：2026-05-13  
状态：PASS

## 覆盖范围

- 新增道具是否进入 `ITEMS`、拾取池和图标映射。
- 玩家是否只能在优势窗口使用优势联动道具。
- 优势道具是否进入 `EncounterState.activeEffects` 并影响后续战斗结算。
- 敌人 AI 是否能在优势修正存在时选择并使用优势联动道具。
- 既有拾取、敌人成长、掉落、情报、地图、视野和战斗回归。

## 自动化结果

- `npm test`：35 项通过，0 项失败。

## 重点断言

- `rib-hook` 等 6 件新增道具存在静态定义。
- 玩家在优势窗口使用 `rib-hook` 后，下一次近战伤害正确增加。
- 敌人拥有 `enemyBonus` 且携带 `rib-hook` 时，会作为战斗动作使用该道具。
- 所有道具定义都能解析到已有 grid dungeon 图标资产。

## 残余风险

- 新增 6 件道具目前复用旧图标；后续若进入美术 polish，应补独立小图标。
- 敌人使用优势道具的权重偏直接，后续可在平衡阶段加入更细的概率与局势判断。
