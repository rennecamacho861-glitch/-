# 战斗日志滚动修正 QA 记录

日期：2026-05-18

## 范围

- 验证长战斗日志不会覆盖或顶走战斗动作按钮。
- 验证回合日志可以在战斗面板内部上下滚动。
- 验证新日志默认显示最新记录，同时仍保留历史记录可读。

## 自动化结果

- `tests/unit/rarity_ui.test.mjs`
  - 检查 `.battle-panel` 具备最大高度限制。
  - 检查 `.rounds` 使用 `overflow-y: auto` 与滚动隔离。
  - 检查 HUD 渲染后将回合日志定位到最新条目。
- 全量 `npm test`：通过，78/78。
- `npm run build`：通过。

## 手动验证建议

- 在浏览器中进入一场持续多轮的战斗，确认动作按钮始终可见可点。
- 鼠标滚轮或触控拖动回合日志区域，确认能查看较早回合记录。
